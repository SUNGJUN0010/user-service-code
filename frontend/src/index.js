

import React from 'react';
import ReactDOM from 'react-dom/client';
import './amplify-config';  // Amplify 설정 import
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);


